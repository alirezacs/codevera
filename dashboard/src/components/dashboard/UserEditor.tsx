"use client";
import Button from "@/components/ui/button/Button";
import ComponentCard from "@/components/common/ComponentCard";
import { api, json } from "@/lib/api";
import { useEffect, useState } from "react";

type User = { id?:string; name:string; email:string; password?:string; role:"ADMIN"|"OWNER"; active:boolean };
export default function UserEditor({id}:{id?:string}) { const [form,setForm]=useState<User>({name:"",email:"",password:"",role:"ADMIN",active:true}); const [error,setError]=useState(""); const [saving,setSaving]=useState(false); useEffect(()=>{if(id)api<{items:User[]}>("admin/users?page=1&limit=100").then(v=>{const user=v.items.find(item=>item.id===id);if(user)setForm({...user,password:""});else setError("User not found.");}).catch(e=>setError(e.message));},[id]); const set=(key:keyof User,value:string|boolean)=>setForm(v=>({...v,[key]:value})); return <ComponentCard title={id?"Edit administrator":"Create administrator"} desc="Administrator access is controlled by role and active status."><form className="grid gap-4 md:grid-cols-2" onSubmit={async e=>{e.preventDefault();setSaving(true);setError("");try{if(id)await api(`admin/users/${id}`,json("PATCH",{name:form.name,role:form.role,active:form.active}));else await api("admin/users",json("POST",{name:form.name,email:form.email,password:form.password,role:form.role}));}catch(e){setError(e instanceof Error?e.message:"Unable to save.");}finally{setSaving(false);}}}><label>Name<input required minLength={2} value={form.name} onChange={e=>set("name",e.target.value)} className="mt-1 w-full rounded-lg border border-gray-300 bg-transparent p-3 dark:border-gray-700"/></label><label>Email<input required type="email" disabled={Boolean(id)} value={form.email} onChange={e=>set("email",e.target.value)} className="mt-1 w-full rounded-lg border border-gray-300 bg-transparent p-3 dark:border-gray-700"/></label>{!id&&<label>Password<input required type="password" minLength={12} value={form.password} onChange={e=>set("password",e.target.value)} className="mt-1 w-full rounded-lg border border-gray-300 bg-transparent p-3 dark:border-gray-700"/></label>}<label>Role<select value={form.role} onChange={e=>set("role",e.target.value)} className="mt-1 w-full rounded-lg border border-gray-300 bg-transparent p-3 dark:border-gray-700"><option>ADMIN</option><option>OWNER</option></select></label>{id&&<label className="flex items-center gap-2 pt-7"><input type="checkbox" checked={form.active} onChange={e=>set("active",e.target.checked)}/> Active account</label>}<div className="md:col-span-2"><Button disabled={saving}>{saving?"Saving…":"Save administrator"}</Button>{error&&<p role="alert" className="mt-3 text-error-500">{error}</p>}</div></form></ComponentCard>; }
