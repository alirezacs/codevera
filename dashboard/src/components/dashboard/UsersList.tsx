"use client";
import ComponentCard from "@/components/common/ComponentCard";
import { Link } from "@/i18n/navigation";
import { api } from "@/lib/api";
import { useEffect, useState } from "react";
type User={id:string;name:string;email:string;role:string;active:boolean;created_at:string};
export default function UsersList(){const [users,setUsers]=useState<User[]>([]);const [error,setError]=useState("");useEffect(()=>{api<{items:User[]}>("admin/users?page=1&limit=100").then(v=>setUsers(v.items)).catch(e=>setError(e.message));},[]);return <ComponentCard title="Administrators" desc="Manage administrator access and roles."><Link href="/users/new" className="inline-flex rounded-lg bg-brand-500 px-4 py-3 text-theme-sm font-medium text-white hover:bg-brand-600">Create administrator</Link>{error&&<p className="mt-3 text-error-500">{error}</p>}<div className="mt-5 overflow-x-auto"><table className="min-w-full text-start text-theme-sm"><thead><tr className="border-b border-gray-200 text-gray-500 dark:border-gray-800"><th className="p-3">Name</th><th className="p-3">Email</th><th className="p-3">Role</th><th className="p-3">Status</th><th className="p-3">Action</th></tr></thead><tbody>{users.map(user=><tr key={user.id} className="border-b border-gray-100 dark:border-gray-800"><td className="p-3 font-medium">{user.name}</td><td className="p-3">{user.email}</td><td className="p-3">{user.role}</td><td className="p-3">{user.active?"Active":"Disabled"}</td><td className="p-3"><Link href={`/users/${user.id}`} className="text-brand-500">Edit</Link></td></tr>)}</tbody></table></div></ComponentCard>}
